meta {
  name: List Addresses
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/member/addresses
  body: none
  auth: bearer
}

query {
  ~search: 
  ~is_default: 
}

auth:bearer {
  token: {{authToken}}
}
