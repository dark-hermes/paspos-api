meta {
  name: Get Stock Movement Detail
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/stock-movements/{{stockMovementId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  followRedirects: true
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("get stock movement detail returns selected movement", function() {
    const body = res.getBody();
    expect(body.status).to.equal("success");
    expect(body.data.id).to.equal(Number(bru.getEnvVar("stockMovementId")));
  });
}
