meta {
  name: "Sales Dashboard Summary"
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/orders/dashboard?store_id={{storeId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

params:query {
  store_id: {{storeId}}
}
