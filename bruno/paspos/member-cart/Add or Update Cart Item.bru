meta {
  name: "Add or Update Cart Item"
  type: "http"
  seq: 2
}

post {
  url: "{{baseUrl}}/api/member/{{branchId}}/cart"
  body: json
  auth: inherit
}

body:json {
  {
    "product_id": 1,
    "quantity": 2
  }
}
