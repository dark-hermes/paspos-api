meta {
  name: "Search Products"
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/pos/products?store_id={{storeId}}&search=Product Name
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

params:query {
  store_id: {{storeId}}
  search: Product Name
}
