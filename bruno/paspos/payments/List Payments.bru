meta {
  name: "List Payments"
  type: "http"
  seq: 2
}

get {
  url: {{baseUrl}}/api/payments?order_id=1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

query {
  order_id: "1"
}
