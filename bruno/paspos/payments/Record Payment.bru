meta {
  name: "Record Payment"
  type: "http"
  seq: 1
}

post {
  url: {{baseUrl}}/api/payments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "order_id": 1,
    "amount": 10000,
    "payment_method": "cash",
    "note": "Payment for debt"
  }
}
