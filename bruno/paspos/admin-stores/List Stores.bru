meta {
  name: List Stores
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/stores
  body: none
  auth: bearer
}

query {
  ~search: 
  ~type: 
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  followRedirects: true
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("list stores returns success", function() {
    const body = res.getBody();
    expect(body.status).to.equal("success");
  });
}
