meta {
  name: Delete Product Category
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/product-categories/{{categoryId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

settings {
  encodeUrl: true
  followRedirects: true
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("delete category returns success", function() {
    const body = res.getBody();
    expect(body.status).to.equal("success");
  });
}
