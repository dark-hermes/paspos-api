meta {
  name: Request Email Update OTP
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/me/email/request-otp
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "new_email": "{{newEmail}}"
  }
}
